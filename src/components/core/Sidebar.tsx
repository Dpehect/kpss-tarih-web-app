"use client";

// Legacy compatibility wrapper. The active navigation is now owned by AppShell.
// Keeping this component lightweight prevents duplicate fixed overlays from blocking clicks.
export function Sidebar() {
  return null;
}
