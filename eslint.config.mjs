import nextVitals from "eslint-config-next/core-web-vitals";

/**
 * Next.js Core Web Vitals kurallarıyla temel lint konfigürasyonu.
 */
const eslintConfig = [...nextVitals];

export default eslintConfig;
