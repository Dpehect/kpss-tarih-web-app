# Supabase paketleri

Projede şu paketleri kur:

```bash
npm install @supabase/supabase-js @supabase/ssr
```

`.env.local.example` dosyasını `.env.local` olarak kopyala:

```bash
cp .env.local.example .env.local
```

Windows PowerShell:

```powershell
Copy-Item .env.local.example .env.local
```
